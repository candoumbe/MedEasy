# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_continuous --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: continuous

on:
  push:
    branches-ignore:
      - main
  pull_request:
    branches:
      - develop
    paths:
      - '!**/*.md'
      - '!LICENCE'
      - '!docs/*'

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Cache .nuke/temp, ~/.nuget/packages
        uses: actions/cache@v3
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('global.json', 'nuget.config', '.config/dotnet-tools.json') }}
      - name: Run './build.cmd Compile IntegrationTests'
        run: ./build.cmd Compile IntegrationTests
        env:
          GitHubToken: ${{ secrets.GITHUB_TOKEN }}
          NugetApiKey: ${{ secrets.NUGET_API_KEY }}
      - uses: actions/upload-artifact@v3
        with:
          name: databases
          path: output/databases
      - uses: actions/upload-artifact@v3
        with:
          name: connections.dat
          path: output/connections.dat
      - uses: actions/upload-artifact@v3
        with:
          name: integration-tests
          path: output/tests-results/integration-tests
      - uses: actions/upload-artifact@v3
        with:
          name: integration-tests
          path: output/coverage-report/integration-tests
