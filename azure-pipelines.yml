# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

trigger:
  branches:
    include:
      - main
  paths:
    exclude:
      - docs/*
      - README.md
      - CHANGELOG.md

stages:
  - stage: windows_latest
    displayName: 'windows-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'windows-latest'
    jobs:
      - job: Compile
        displayName: 'Compile'
        dependsOn: [  ]
        steps:
          - task: Cache@2.198.0
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2.201.1
            inputs:
              script: './build.cmd Restore Compile --skip'
      - job: CleanDatabaseFolder
        displayName: 'CleanDatabaseFolder'
        dependsOn: [  ]
        steps:
          - task: Cache@2.198.0
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2.201.1
            inputs:
              script: './build.cmd CleanDatabaseFolder --skip'
      - job: UpdateDatabases
        displayName: 'UpdateDatabases'
        dependsOn: [ Compile, CleanDatabaseFolder ]
        steps:
          - task: Cache@2.198.0
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2.201.1
            inputs:
              script: './build.cmd UpdateDatabases --skip'
          - task: PublishBuildArtifacts@1.211.0
            inputs:
              artifactName: databases
              pathtoPublish: 'output/databases'
      - job: IntegrationTests
        displayName: 'IntegrationTests'
        dependsOn: [ Compile, UpdateDatabases ]
        steps:
          - task: Cache@2.198.0
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2.201.1
            inputs:
              script: './build.cmd IntegrationTests --skip'
          - task: PublishBuildArtifacts@1.211.0
            inputs:
              artifactName: integration-tests
              pathtoPublish: 'output/tests-results/integration-tests'
          - task: PublishBuildArtifacts@1.211.0
            inputs:
              artifactName: integration-tests
              pathtoPublish: 'output/coverage-report/integration-tests'
      - job: UnitTests
        displayName: 'UnitTests'
        dependsOn: [ Compile ]
        steps:
          - task: Cache@2.198.0
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2.201.1
            inputs:
              script: './build.cmd UnitTests --skip'
          - task: PublishBuildArtifacts@1.211.0
            inputs:
              artifactName: unit-tests
              pathtoPublish: 'output/tests-results/unit-tests'
          - task: PublishBuildArtifacts@1.211.0
            inputs:
              artifactName: unit-tests
              pathtoPublish: 'output/coverage-report/unit-tests'
      - job: Tests
        displayName: 'Tests'
        dependsOn: [ UnitTests, IntegrationTests ]
        steps:
          - task: Cache@2.198.0
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2.201.1
            inputs:
              script: './build.cmd Tests --skip'
      - job: Pack
        displayName: 'Pack'
        dependsOn: [ Tests, Compile ]
        steps:
          - task: Cache@2.198.0
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2.201.1
            inputs:
              script: './build.cmd Pack --skip'
          - task: PublishBuildArtifacts@1.211.0
            inputs:
              artifactName: artifacts
              pathtoPublish: 'output/artifacts'
