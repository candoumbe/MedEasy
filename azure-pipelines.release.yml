# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines_release --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

trigger:
  branches:
    include:
      - release/*
  paths:
    exclude:
      - docs/*
      - README.md
      - CHANGELOG.md

stages:
  - stage: windows_latest
    displayName: 'windows-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'windows-latest'
    jobs:
      - job: Compile
        displayName: 'Compile'
        dependsOn: [  ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd Restore Compile --skip'
      - job: UnitTests
        displayName: 'UnitTests'
        dependsOn: [ Compile ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd UnitTests --skip'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: unit-tests
              pathtoPublish: 'output/tests-results/unit-tests'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: unit-tests
              pathtoPublish: 'output/coverage-report/unit-tests'
      - job: Pack
        displayName: 'Pack'
        dependsOn: [ UnitTests, Compile ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd Pack --skip'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: artifacts
              pathtoPublish: 'output/artifacts'
