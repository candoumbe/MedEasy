namespace MedEasy.Web.Constants
{
    /// <summary>
    /// Constant key names used to use when storing/retrieving value to (resp. from) local or session storage.
    /// </summary>
    public class StorageKeyNames
    {
        /// <summary>
        /// Name of the key used to store the JWT token
        /// </summary>
        public static readonly string Token = nameof(Token);
    }
}
