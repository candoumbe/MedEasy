declare namespace MedEasy.DTO {

    export interface BearerTokenInfo {
        /**
         * Access token
         */
        accessToken: string;
        /**
         * Refresh token
         */
        refreshToken: string;
    }

}