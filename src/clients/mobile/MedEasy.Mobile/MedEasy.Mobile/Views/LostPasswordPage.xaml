<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MedEasy.Mobile.Views.LostPasswordPage"
             xmlns:vm="clr-namespace:MedEasy.Mobile.Core.ViewModels.Base"
             BindingContext="{x:Static vm:ViewModelLocator.LostPasswordViewModel}">

    <StackLayout VerticalOptions="CenterAndExpand" 
                 HorizontalOptions="FillAndExpand"
                 Margin="10, 5">
    
        <Label Text="Nom d'utilisateur ou adresse mail"/>
        <Entry Keyboard="Email" Text="{Binding Email}"/>
        <Button Text="Réinitialiser" Command="{Binding ResetPasswordCommand}"/>
    
    </StackLayout>

</ContentPage>